# Presenter-only solution reference

The intentional bug is here:

```js
// src/pricing.js
SAVE20: 0.02,
```

The correct value is:

```js
SAVE20: 0.20,
```

Do not reveal this file before the TestSprite failure bundle is shown. The strongest recording lets the coding agent discover and patch it from the failure evidence.
